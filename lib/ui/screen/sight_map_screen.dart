import 'package:flutter/material.dart';

class SightMapScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Здесь скоро будет карта'),
      ),
    );
  }
}
